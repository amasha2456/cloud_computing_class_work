apiVersion: apps/v1
kind: Deployment
metadata:
  name: registration-service-blue
  namespace: newevent
  labels:
    app: registration-service
    slot: blue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: registration-service
      slot: blue
  template:
    metadata:
      labels:
        app: registration-service
        slot: blue
    spec:
      containers:
        - name: registration-service
          image: newevent/registration-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5003
          env:
            - name: PORT
              value: "5003"
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          readinessProbe:
            httpGet: { path: /health, port: 5003 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 5003 }
            initialDelaySeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registration-service-green
  namespace: newevent
  labels:
    app: registration-service
    slot: green
spec:
  replicas: 0
  selector:
    matchLabels:
      app: registration-service
      slot: green
  template:
    metadata:
      labels:
        app: registration-service
        slot: green
    spec:
      containers:
        - name: registration-service
          image: newevent/registration-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5003
          env:
            - name: PORT
              value: "5003"
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          readinessProbe:
            httpGet: { path: /health, port: 5003 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 5003 }
            initialDelaySeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: registration-service
  namespace: newevent
spec:
  selector:
    app: registration-service
    slot: blue
  ports:
    - port: 5003
      targetPort: 5003
