apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-service-blue
  namespace: newevent
  labels:
    app: email-service
    slot: blue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: email-service
      slot: blue
  template:
    metadata:
      labels:
        app: email-service
        slot: blue
    spec:
      containers:
        - name: email-service
          image: newevent/email-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5004
          env:
            - name: PORT
              value: "5004"
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          readinessProbe:
            httpGet: { path: /health, port: 5004 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 5004 }
            initialDelaySeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-service-green
  namespace: newevent
  labels:
    app: email-service
    slot: green
spec:
  replicas: 0
  selector:
    matchLabels:
      app: email-service
      slot: green
  template:
    metadata:
      labels:
        app: email-service
        slot: green
    spec:
      containers:
        - name: email-service
          image: newevent/email-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5004
          env:
            - name: PORT
              value: "5004"
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          readinessProbe:
            httpGet: { path: /health, port: 5004 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 5004 }
            initialDelaySeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: email-service
  namespace: newevent
spec:
  selector:
    app: email-service
    slot: blue
  ports:
    - port: 5004
      targetPort: 5004
